global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "stratisio-backend"
    static_configs:
      - targets: ["host.docker.internal:8001"] # Assuming local dev or docker-compose

  - job_name: "kubernetes-nodes"
    kubernetes_sd_configs:
      - role: node
